# File ini dipertahankan untuk kompatibilitas (VPS deployment).
# Requirements dipindah ke requirements/ subfolder.
#
# VPS / production:
#   pip install -r requirements.txt          ← file ini (sama dengan base)
#
# Local development:
#   pip install -r requirements/dev.txt      ← base + ruff + mypy + pytest

-r requirements/base.txt
