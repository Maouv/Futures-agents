# ── Secrets (NEVER commit this file with real values) ─────────────────────
# Copy to .env and fill in your values: cp .env.example .env

# Binance Futures (Production) — required when live + USE_TESTNET=false
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Binance Testnet — required when live + USE_TESTNET=true
BINANCE_TESTNET_KEY=
BINANCE_TESTNET_SECRET=

# LLM API Keys — required
CEREBRAS_API_KEY=
GROQ_API_KEY=
CONCIERGE_API_KEY=

# Telegram — required
TELEGRAM_BOT_TOKEN=

